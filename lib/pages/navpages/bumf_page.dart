import 'package:flutter/cupertino.dart';

class BumfPage extends StatelessWidget {
  const BumfPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Bumf Page')),
    );
  }
}